import { OptionModel } from "../../components/common/Select/Select.models";

export interface BuilderPagePropsModel {
  
}

export interface BuilderPageStateModel {
  redirectPath: string;
  errorMessage: string;
}